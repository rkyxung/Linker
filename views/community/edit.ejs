<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Linker</title>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Pretendard:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/main.css" />
  <link rel="stylesheet" href="/css/forms.css" />
</head>

<body>
  <% const displayName=currentUser && currentUser.nickname ? currentUser.nickname : currentUser && currentUser.name ?
    currentUser.name : "링커" ; %>

    <%- include('../include/_nav', { active: 'community', displayName: displayName }) %>

      <main class="main-content" aria-label="메인 콘텐츠">
        <div class="form-container">
          <a href="/community/<%= post._id %>" class="back-link">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M10 12L6 8L10 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            뒤로가기
          </a>

          <h1 class="form-title">
            커뮤니티 글 수정
          </h1>

          <% if (typeof error !== "undefined" && error) { %>
            <div class="error-message" role="alert">
              <%= error %>
            </div>
          <% } %>

          <form action="/community/<%= post._id %>/edit" method="POST">
            <div class="form-group">
              <label for="title">제목</label>
              <input type="text" id="title" name="title" placeholder="글 제목을 입력하세요" 
                value="<%= post.title %>" 
                required />
            </div>

            <div class="form-group">
              <label for="category">게시글 종류</label>
              <select id="category" name="category" required>
                <option value="free" <%= post.category === 'free' ? 'selected' : '' %>>자유게시판</option>
                <option value="qna" <%= post.category === 'qna' ? 'selected' : '' %>>Q&A</option>
                <option value="info" <%= post.category === 'info' ? 'selected' : '' %>>정보공유</option>
              </select>
            </div>

            <div class="form-group">
              <label for="content">내용</label>
              <textarea id="content" name="content" placeholder="글 내용을 입력하세요" required><%= post.content %></textarea>
            </div>

            <div class="form-actions">
              <button type="button" class="btn btn-secondary" onclick="window.location.href='/community/<%= post._id %>'">취소</button>
              <button type="submit" class="btn btn-primary">수정</button>
            </div>
          </form>
        </div>
      </main>
</body>

</html>

