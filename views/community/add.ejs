<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Linker</title>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Pretendard:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/main.css" />
  <style>
    .form-container {
      background: transparent;
      border-radius: 1.67vw;
      padding: 1.5vw 2vw;
      border: none;
      backdrop-filter: none;
      -webkit-backdrop-filter: none;
    }

    .form-group {
      margin-bottom: 1.2vw;
    }

    .form-group label {
      display: block;
      margin-bottom: 0.5vw;
      font-size: 1.04vw;
      font-weight: 600;
      color: rgba(24, 50, 117, 0.92);
    }

    .form-group input[type="text"],
    .form-group textarea {
      width: 100%;
      padding: 0.83vw 1.04vw;
      border-radius: 0.94vw;
      border: 0.05vw solid rgba(44, 120, 255, 0.25);
      background: rgba(255, 255, 255, 0.8);
      font-size: 0.94vw;
      color: #1f3d7a;
      font-family: "Pretendard", sans-serif;
      box-sizing: border-box;
    }

    .form-group input[type="text"]:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: #2f6bff;
      background: rgba(255, 255, 255, 0.95);
      box-shadow: none;
    }

    .form-group textarea {
      min-height: 15vw;
      resize: vertical;
    }

    .form-actions {
      display: flex;
      gap: 1vw;
      justify-content: flex-end;
      margin-top: 1.5vw;
    }

    .btn {
      padding: 0.73vw 1.46vw;
      border-radius: 0.94vw;
      border: none;
      font-size: 0.94vw;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s ease;
      font-family: "Pretendard", sans-serif;
    }

    .btn-primary {
      background: linear-gradient(120deg, #2f6bff, #4ea6ff);
      color: #ffffff;
      box-shadow: none;
    }

    .btn-primary:hover {
      background: linear-gradient(120deg, #2560e6, #3d96e6);
      box-shadow: none;
    }

    .btn-secondary {
      background: rgba(255, 255, 255, 0.6);
      color: #1f3d7a;
      border: 0.05vw solid rgba(44, 120, 255, 0.25);
    }

    .btn-secondary:hover {
      background: rgba(255, 255, 255, 0.8);
    }

    .error-message {
      background: rgba(255, 59, 59, 0.1);
      color: #ff3b3b;
      padding: 0.83vw 1.04vw;
      border-radius: 0.94vw;
      margin-bottom: 1.5vw;
      font-size: 0.88vw;
      border: 0.05vw solid rgba(255, 59, 59, 0.2);
    }

    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5vw;
      color: rgba(24, 50, 117, 0.8);
      text-decoration: none;
      font-size: 0.94vw;
      font-weight: 600;
      margin-bottom: 1.5vw;
      transition: color 0.2s ease;
    }

    .back-link:hover {
      color: #1d5dff;
    }
  </style>
</head>

<body>
  <% const displayName=currentUser && currentUser.nickname ? currentUser.nickname : currentUser && currentUser.name ?
    currentUser.name : "링커" ; %>

    <%- include('../include/_nav', { active: 'community', displayName: displayName }) %>

      <main class="main-content" aria-label="메인 콘텐츠">
        <div class="form-container">
          <a href="/community" class="back-link">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M10 12L6 8L10 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            뒤로가기
          </a>

          <h1 style="font-size: 1.46vw; font-weight: 700; color: rgba(24, 50, 117, 0.92); margin-bottom: 1.5vw;">
            커뮤니티 글 작성
          </h1>

          <% if (typeof error !== "undefined" && error) { %>
            <div class="error-message" role="alert">
              <%= error %>
            </div>
          <% } %>

          <form action="/community/add" method="POST">
            <div class="form-group">
              <label for="title">제목</label>
              <input type="text" id="title" name="title" placeholder="글 제목을 입력하세요" 
                value="<%= typeof formData !== 'undefined' && formData && formData.title ? formData.title : '' %>" 
                required />
            </div>

            <div class="form-group">
              <label for="category">게시글 종류</label>
              <select id="category" name="category" required style="width: 100%; padding: 0.83vw 1.04vw; border-radius: 0.94vw; border: 0.05vw solid rgba(44, 120, 255, 0.25); background: rgba(255, 255, 255, 0.8); font-size: 0.94vw; color: #1f3d7a; font-family: 'Pretendard', sans-serif; box-sizing: border-box;">
                <option value="free" <%= typeof formData !== 'undefined' && formData && formData.category === 'free' ? 'selected' : '' %>>자유게시판</option>
                <option value="qna" <%= typeof formData !== 'undefined' && formData && formData.category === 'qna' ? 'selected' : '' %>>Q&A</option>
                <option value="info" <%= typeof formData !== 'undefined' && formData && formData.category === 'info' ? 'selected' : '' %>>정보공유</option>
              </select>
            </div>

            <div class="form-group">
              <label for="content">내용</label>
              <textarea id="content" name="content" placeholder="글 내용을 입력하세요" required><%= typeof formData !== 'undefined' && formData && formData.content ? formData.content : '' %></textarea>
            </div>

            <div class="form-actions">
              <button type="button" class="btn btn-secondary" onclick="window.location.href='/community'">취소</button>
              <button type="submit" class="btn btn-primary">등록</button>
            </div>
          </form>
        </div>
      </main>
</body>

</html>

