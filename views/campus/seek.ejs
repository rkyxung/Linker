<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Linker</title>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Pretendard:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/main.css" />
  <link rel="stylesheet" href="/css/forms.css" />
</head>

<body>
  <% const displayName=currentUser && currentUser.nickname ? currentUser.nickname : currentUser && currentUser.name ?
    currentUser.name : "링커" ; %>

    <%- include('../include/_nav', { active: 'campus', displayName: displayName }) %>

      <main class="main-content" aria-label="메인 콘텐츠">
        <div class="form-container">
          <a href="/campus" class="back-link">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M10 12L6 8L10 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            뒤로가기
          </a>

          <h1 class="form-title">
            <%= typeof editMode !== 'undefined' && editMode ? '팀 구하기 글 수정' : '팀 구하기' %>
          </h1>

          <% if (typeof error !== "undefined" && error) { %>
            <div class="error-message" role="alert">
              <%= error %>
            </div>
          <% } %>

          <form action="<%= typeof formAction !== 'undefined' ? formAction : '/campus/seek' %>" method="POST">
            <div class="form-group">
              <label for="title">제목</label>
              <input type="text" id="title" name="title" placeholder="제목을 입력하세요" 
                value="<%= typeof formData !== 'undefined' && formData && formData.title ? formData.title : '' %>" 
                required />
            </div>

            <div class="form-group">
              <label for="category">카테고리</label>
              <select id="category" name="category" required>
                <option value="project" <%= typeof formData !== 'undefined' && formData && formData.category === 'project' ? 'selected' : '' %>>프로젝트</option>
                <option value="study" <%= typeof formData !== 'undefined' && formData && formData.category === 'study' ? 'selected' : '' %>>스터디</option>
              </select>
            </div>

            <div class="form-group">
              <label for="desiredFields">원하는 분야</label>
              <input type="text" id="desiredFields" name="desiredFields" placeholder="예: 웹 개발, 모바일 앱, AI/ML (쉼표로 구분)" 
                value="<%= typeof formData !== 'undefined' && formData && formData.desiredFields ? (Array.isArray(formData.desiredFields) ? formData.desiredFields.join(', ') : formData.desiredFields) : '' %>" />
              <small>참여하고 싶은 분야를 쉼표로 구분하여 입력하세요</small>
            </div>

            <div class="form-group">
              <label for="skills">보유 기술/스킬</label>
              <input type="text" id="skills" name="skills" placeholder="예: React, Node.js, Python (쉼표로 구분)" 
                value="<%= typeof formData !== 'undefined' && formData && formData.skills ? (Array.isArray(formData.skills) ? formData.skills.join(', ') : formData.skills) : '' %>" />
              <small>보유하고 있는 기술이나 스킬을 쉼표로 구분하여 입력하세요</small>
            </div>

            <div class="form-group">
              <label for="desiredPosition">원하는 포지션</label>
              <input type="text" id="desiredPosition" name="desiredPosition" placeholder="예: 프론트엔드 개발자, 백엔드 개발자" 
                value="<%= typeof formData !== 'undefined' && formData && formData.desiredPosition ? formData.desiredPosition : '' %>" />
            </div>

            <div class="form-group">
              <label for="experience">경력/경험</label>
              <textarea id="experience" name="experience" placeholder="관련 경력이나 프로젝트 경험을 입력하세요"><%= typeof formData !== 'undefined' && formData && formData.experience ? formData.experience : '' %></textarea>
            </div>

            <div class="form-group">
              <label for="hashtags">해시태그</label>
              <input type="text" id="hashtags" name="hashtags" placeholder="예: # 팀 구함, # 사이드프로젝트 (쉼표로 구분)" 
                value="<%= typeof formData !== 'undefined' && formData && formData.hashtags ? (Array.isArray(formData.hashtags) ? formData.hashtags.map(h => '#' + h).join(', ') : formData.hashtags) : '' %>" />
              <small>쉼표로 구분하여 입력하세요. # 기호는 자동으로 추가됩니다</small>
            </div>

            <div class="form-group">
              <label for="content">자기소개 및 PR</label>
              <textarea id="content" name="content" placeholder="자신을 어필할 수 있는 내용을 작성해주세요" required><%= typeof formData !== 'undefined' && formData && formData.content ? formData.content : '' %></textarea>
            </div>

            <div class="form-actions">
              <button type="button" class="btn btn-secondary" onclick="window.location.href='/campus'">취소</button>
              <button type="submit" class="btn btn-primary">등록</button>
            </div>
          </form>
        </div>
      </main>
</body>

</html>

