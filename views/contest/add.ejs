<!DOCTYPE html>
<html lang="ko">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Linker</title>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Pretendard:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/css/main.css" />
  <link rel="stylesheet" href="/css/forms.css" />
</head>

<body>
  <% const displayName=currentUser && currentUser.nickname ? currentUser.nickname : currentUser && currentUser.name ?
    currentUser.name : "링커" ; %>

    <%- include('../include/_nav', { active: 'contest', displayName: displayName }) %>

      <main class="main-content" aria-label="메인 콘텐츠">
        <div class="form-container">
          <a href="/contest" class="back-link">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
              <path d="M10 12L6 8L10 4" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
            뒤로가기
          </a>

          <h1 class="form-title">
            <%= typeof editMode !== 'undefined' && editMode ? '팀원 모집 글 수정' : '팀원 모집하기' %>
          </h1>

          <% if (typeof error !== "undefined" && error) { %>
            <div class="error-message" role="alert">
              <%= error %>
            </div>
          <% } %>

          <form action="<%= typeof formAction !== 'undefined' ? formAction : '/contest/add' %>" method="POST">
            <div class="form-group">
              <label for="title">제목</label>
              <input type="text" id="title" name="title" placeholder="모집글 제목을 입력하세요" 
                value="<%= typeof formData !== 'undefined' && formData && formData.title ? formData.title : '' %>" 
                required />
            </div>

            <div class="form-group">
              <label for="category">카테고리</label>
              <select id="category" name="category" required>
                <option value="design" <%= typeof formData !== 'undefined' && formData && formData.category === 'design' ? 'selected' : '' %>>디자인</option>
                <option value="develop" <%= typeof formData !== 'undefined' && formData && formData.category === 'develop' ? 'selected' : '' %>>개발</option>
                <option value="planning" <%= typeof formData !== 'undefined' && formData && formData.category === 'planning' ? 'selected' : '' %>>기획</option>
              </select>
            </div>

            <div class="form-group">
              <label for="deadline">모집 마감일</label>
              <input type="date" id="deadline" name="deadline" 
                value="<%= typeof formData !== 'undefined' && formData && formData.deadline ? formData.deadline : '' %>" 
                required />
            </div>

            <div class="form-group">
              <label for="recruitCount">모집 인원 수</label>
              <input type="number" id="recruitCount" name="recruitCount" min="1" 
                value="<%= typeof formData !== 'undefined' && formData && formData.recruitCount ? formData.recruitCount : '' %>" 
                required />
            </div>

            <div class="form-group">
              <label for="positions">포지션</label>
              <input type="text" id="positions" name="positions" placeholder="예: 프론트엔드, 백엔드, 디자이너 (쉼표로 구분)" 
                value="<%= typeof formData !== 'undefined' && formData && formData.positions ? (Array.isArray(formData.positions) ? formData.positions.join(', ') : formData.positions) : '' %>" />
              <small>쉼표로 구분하여 입력하세요</small>
            </div>

            <div class="form-group">
              <label for="hashtags">해시태그</label>
              <input type="text" id="hashtags" name="hashtags" placeholder="예: # 팀원 구함, # 사이드프로젝트 (쉼표로 구분)" 
                value="<%= typeof formData !== 'undefined' && formData && formData.hashtags ? (Array.isArray(formData.hashtags) ? formData.hashtags.map(h => '#' + h).join(', ') : formData.hashtags) : '' %>" />
              <small>쉼표로 구분하여 입력하세요. # 기호는 자동으로 추가됩니다</small>
            </div>

            <div class="form-group">
              <label for="content">내용</label>
              <textarea id="content" name="content" placeholder="모집글 내용을 입력하세요" required><%= typeof formData !== 'undefined' && formData && formData.content ? formData.content : '' %></textarea>
            </div>

            <div class="form-actions">
              <button type="button" class="btn btn-secondary" onclick="window.location.href='/contest'">취소</button>
              <button type="submit" class="btn btn-primary">등록</button>
            </div>
          </form>
        </div>
      </main>
</body>

</html>

